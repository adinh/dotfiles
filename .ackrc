# Options
--smart-case
--sort-files

# Exclusions
--ignore-file=is:tags
--ignore-dir=is:build
--ignore-dir=is:coverage
--ignore-dir=is:log
--ignore-dir=is:target
--ignore-dir=is:tmp
--ignore-dir=is:vendor
